import AppLayout from '@/components/app/app-layout';
import ExpenseManagement from '@/components/app/expense-management';

export default function ExpensesPage() {
  return (
    <AppLayout>
      <ExpenseManagement />
    </AppLayout>
  );
}
